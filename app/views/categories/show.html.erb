<p id="notice"><%= notice %></p>

<p>
  <strong>Id:</strong>
  <%= @category.id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Root category:</strong>
  <% if root_category = Category.find_by(id: @category.root_category_id)%>
    <%= root_category.name %>
  <% end %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
